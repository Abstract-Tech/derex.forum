version: "3.5"

services:
  forum:
    image: derex/forum-ironwood
    environment:
      API_KEY: "forumapikey"
      SEARCH_SERVER: "http://elasticsearch:9200"
      MONGOHQ_URL: "mongodb://mongodb:27017/{{ project.name }}_forum"
    networks:
      derex:
